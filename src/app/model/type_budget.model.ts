

// Déclaration de la classe Type_Budget (modèle côté frontend)
export class Type_Budget {

  // Identifiant du type d'impôt (optionnel car généré par la base de données)
  idType_Budget?: number;

  // Description du type de budget (libellé)
  budgetTypeDesc?: string;

}